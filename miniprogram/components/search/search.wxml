<!--components/search/search.wxml-->
<view class="container">
    <!-- iconfont icon-sousuo 这两个样式外部传入-->
    <i class="iconfont icon-sousuo find"/>
    <input class="bar" placeholder="{{placeholder}}" placeholder-class="in-bar" bind:input="onInput"></input>
    <button size="mini" class="search" bind:tap="onSearch">搜索</button>
</view>
